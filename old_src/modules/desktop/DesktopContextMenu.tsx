import React from 'react'

const DesktopContextMenu: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  // Qui implementerai il menu contestuale (click destro)
  return (
    <div>
      {children}
    </div>
  )
}

export default DesktopContextMenu
