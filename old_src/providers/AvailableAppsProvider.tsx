import React from 'react'

export const AvailableAppsProvider = ({ children }: { children?: React.ReactNode }) => {
  // in futuro fornirà la lista delle app installate
  return <>{children}</>
}
